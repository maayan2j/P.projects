/*let jo = false
let count = 6;

let counter = setInterval(timer, 1000); //1000 will  run it every 1 second
function timer() {
  count = count - 1;
  if (count <= 0) {
    clearInterval(counter);
    /*  $('#meadle').text(`oops no more frogs for you!!`) */
  /*  render.renderClearInterval()
    return;
  } else if (jo) {

    clearInterval(counter);
  }
  render.timer()
  /*  $('#top').text(`${count} secs left`)
   $('#top').css({color: "rgba(" + Math.floor(Math.random() * 256) + "," + Math.floor(Math.random() * 256) + "," + Math.floor(Math.random() * 256) +")"})
   }  */
 




/* $('#test').css({"width":`${width}`,"height":`${width}`,'left':`${left}`,'top':`${htht}`}) */
/* $('#test').css("height",`${width}`) */
/* let width = Math.floor(Math.random() * 4 * 5) + 5
const meadleHight = $('.meadle').height()
const meadleWidth = $('.meadle').width()
let maxTop = meadleHight - width
let maxLeft = meadleWidth - width
let left = Math.floor(Math.random() * maxLeft + 1)
let htht = Math.floor(Math.random() * maxTop + 1)
$('.meadle').css({ position: "relative" })
$('#test').css({ width: width, height: width, left: left, top: htht, position: 'absolute' })
 */
/* const touchFrog = $('#test').on('click', function () {
  jo = true
  let width = Math.floor(Math.random() * 4 * 5) + 5
  const meadleHight = $('.meadle').height()
  const meadleWidth = $('.meadle').width()
  let maxTop = meadleHight - width
  let maxLeft = meadleWidth - width
  let left = Math.floor(Math.random() * maxLeft + 1)
  let htht = Math.floor(Math.random() * maxTop + 1)
  $('.meadle').css({ position: "relative" })
  $('#test').css({ width: width, height: width, left: left, top: htht, position: 'absolute' })
})

 */



/* $('#test').css({'left':`${left}`,'top':`${htht}`}) */
/* $('#test').css('top',`${htht}`) */
/* console.log($('#test'))
console.log(htht) */
/*
$('#test').on('click',function(){
let width=Math.floor(Math.random()*4*5)+5
$(this).css("width",`${width}`)
$(this).css("height",`${width}`)

}) */







/*
$('#test').click(function() {
   const docHeight = $(".meadle").height(),
        docWidth = $(".meadle").width(),
        $div = $('#test'),
        divWidth = $div.width(),
        divHeight = $div.height(),
        heightMax = docHeight - divHeight,
        widthMax = docWidth - divWidth;

    $div.css({
        left: Math.floor( Math.random() * widthMax ),
        top: Math.floor( Math.random() * heightMax )
    });
});  */